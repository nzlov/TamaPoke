# TamaPoke

[English](README.md) | 简体中文

[![在浏览器中刷写](https://img.shields.io/badge/flash-in%20browser-FF6B00?logo=googlechrome&logoColor=white)](https://dylanpdao.github.io/TamaPoke/web/)
[![MakerWorld](https://img.shields.io/badge/MakerWorld-3D%20case-00AE42?logo=bambulab&logoColor=white)](https://makerworld.com/es/models/2937822-tamapoke-a-pokemon-pokeball-tamagotchi)
![开发板](https://img.shields.io/badge/board-ESP32--S3%20round%20AMOLED-E7352C?logo=espressif&logoColor=white)
![固件](https://img.shields.io/badge/firmware-v3.6-8A2BE2)
![代码](https://img.shields.io/badge/code-MIT-blue)
![语言](https://img.shields.io/badge/languages-7-FFCB05)
[![Stars](https://img.shields.io/github/stars/DylanPDao/TamaPoke?style=flat&logo=github&color=yellow)](https://github.com/DylanPDao/TamaPoke/stargazers)

一款受第一世代宝可梦启发、运行在 **Waveshare
ESP32-S3-Touch-AMOLED-1.75** 上的电子宠物。设备采用 466×466 圆形 AMOLED，
CO5300 QSPI 显示驱动和 CST9217 I2C 触控。你可以培育任意已安装物种，让它进化、
训练它，并完成包含异色形态在内的全部收集。

> ### 🙏 本项目派生自 Quique Tortosa 的 [**socquique/TamaPoke**](https://github.com/socquique/TamaPoke)
>
> Quique 完成了最初的固件、精灵流水线、六语言界面和网页安装器。本分支是在其成果上
> 继续开发，并非从零开始。如果你喜欢本项目，请为
> **[原项目加星](https://github.com/socquique/TamaPoke)**。
>
> 原作：MIT © 2026 Quique Tortosa。本分支的修改以相同条款采用 MIT 许可。

> **个人、非商业同人项目。** 代码采用 MIT；精灵来自 PMD SpriteCollab
>（CC BY-NC，Pokémon © Nintendo/Game Freak）；3D 外壳采用 CC BY-NC-SA。
> 详见[许可证](#许可证)与[致谢](#致谢)。

🔴 **3D 打印精灵球外壳与打印配置 → [MakerWorld](https://makerworld.com/es/models/2937822-tamapoke-a-pokemon-pokeball-tamagotchi)** · 在浏览器中刷写 → **[网页安装器](https://dylanpdao.github.io/TamaPoke/web/)**

## 最新版本与分包说明

当前版本覆盖关都、城都、丰缘和神奥，共 **493 种宝可梦及其异色形态**，并提供
完整的简体中文界面、名称和说明。推荐使用**[网页安装器](https://dylanpdao.github.io/TamaPoke/web/)**：
先刷入固件，再将所选语言与地区部署到 microSD，无需 Arduino IDE。

语言、招式、地区和题库内容现已拆分为可独立部署的运行时数据包。你可以只安装所需语言和
地区，也能在不重新编译固件的情况下更新内容：

| 分包 | 内容 | 是否必需 |
|---|---|---|
| UI（`.tui`） | 界面文字、布局参数和字体；简体中文包为 `ui-zh-CN.tui` | 至少一个 |
| 招式（`.tmove`） | 招式机制、学习表、属性克制、名称和说明 | 必需 |
| 地区（`.tregion`） | 宝可梦资料、名称与说明、动画/异色精灵、缩略图、训练家、道馆和徽章 | 至少一个 |
| 题库（`.tquiz`） | 按语言建立随机访问索引的选择题 | 可选；仅在四则运算题已开启时回退生成 |

安装器读取 `web/packs/index.json`，并根据设备上已有的包与本次所选包检查每项依赖。
如果存在缺失，会明确列出依赖项，并允许取消或显式强制进行部分部署。卡中没有的地区
会保持锁定，也不会进入孵蛋池。固件启动时校验每个包的 ABI 和 CRC；缺少必需包时会
进入内置 USB 恢复界面，而不会加载残缺图鉴；中断上传也不会覆盖可用包。

连接设备后，安装器还会列出已部署包的 ID 与版本，可逐包删除或格式化 microSD。
删除和格式化均需确认；修改存储卡内容后请重启设备。

> **升级提醒：** 首次迁移到运行时分包会清除旧固件存档。此后在相同存档格式间更新，
> 不要勾选 **Erase device** 即可保留存档；刷写固件不会删除 microSD 上的数据包。
> 地区包较大，约 40 MB 的包通过 USB 串口通常需要 10–15 分钟。部署完成后请重启设备。

自行生成与验证分包的方法见[自行生成并加载数据包](#自行生成并加载数据包)和
[运行时数据包](#运行时数据包)。

## 屏幕截图

所有截图均由模拟器以无界面模式直接渲染 466×466 圆屏画面
（`tools/emu/tamapoke-emu --shot <name>`），与硬件实际绘制结果一致。

### 开始游戏

| 选择地区 | 再选择初始伙伴 | 城都御三家 |
|---|---|---|
| <img src="docs/screens/region.png" width="240"> | <img src="docs/screens/starter.png" width="240"> | <img src="docs/screens/starterj.png" width="240"> |

新游戏会先询问地区，再选择该地区的三只初始伙伴之一；之后的蛋也默认来自该地区，
可稍后通过蛋下方的地区标签修改。已有存档不会看到此页面，它只在图鉴为空时出现。

### 培育伙伴

| 你的伙伴 | 它的蛋 | 已学招式 |
|---|---|---|
| <img src="docs/screens/main.png" width="240"> | <img src="docs/screens/egg.png" width="240"> | <img src="docs/screens/moves.png" width="240"> |

蛋带有**地区标签**，可选择关都、城都、丰缘或全部地区。切换地区会保留蛋已抽中的
稀有度，并记住本枚蛋在每个地区的结果，因此不能靠反复切换刷传说宝可梦。

### 对战

| 战斗 | 选择队伍 | 获胜 |
|---|---|---|
| <img src="docs/screens/btlmenu.png" width="240"> | <img src="docs/screens/pick.png" width="240"> | <img src="docs/screens/win.png" width="240"> |

回合制、按招式行动，采用真实属性克制、异常状态和 STAB；全程播放真实 Game Boy
战斗音乐。每次选择本方招式后会先答题：伤害招式按答题阶段缩放最终伤害；变化招式与
治疗招式只要答对就完整生效，答错或超时则整次招式失败。对手 AI 始终按 100% 行动。
局域网对战中双方各自在自己的设备上答题，答题比例随招式发送，由主机统一权威结算；
答题期间持续发送保活帧，因此较长的全局答题限时不会被误判为掉线。

### 四个地区

| 选择挑战路线 | 城都道馆 | 局域网对战 |
|---|---|---|
| <img src="docs/screens/gympick.png" width="240"> | <img src="docs/screens/gymsj.png" width="240"> | <img src="docs/screens/lanready.png" width="240"> |

关都、城都、丰缘和神奥各有八位道馆馆主、四天王与冠军，并提供简单/困难难度。队伍
来自原作，并与 pokecrystal、pokeemerald、pokeplatinum 反编译资料核对；39 位训练家
共 **0 处差异**，可用 `tools/verify_rosters.py` 复查。

神奥采用**白金版**设定，梅丽莎是第三位而非钻石/珍珠中的第五位馆主，因此等级曲线
为 14/22/26/32/37/41/44/50；丰缘同理全程采用绿宝石版。

### 收集

| 选择地区 | 关都 | 城都 |
|---|---|---|
| <img src="docs/screens/dexpick.png" width="240"> | <img src="docs/screens/gallery.png" width="240"> | <img src="docs/screens/gallery2.png" width="240"> |

| 训练家卡片 | 城都徽章 | 盒子 |
|---|---|---|
| <img src="docs/screens/player.png" width="240"> | <img src="docs/screens/player2.png" width="240"> | <img src="docs/screens/box.png" width="240"> |

## 当前状态

已在硬件上运行。已实现：493 种及异色形态的地区包动画、完整生命周期（按稀有度
孵蛋 → 进化 → 告别/放生/出走，均由决定对话框触发）、培育图鉴、战斗数值（IV 与
训练）、连续照顾/羁绊/勋章/昵称、生态与实时时间背景、球类小游戏、训练沙袋、答题
弹窗、索引题库、可配置四则运算、洗澡
动画、RTC 离线进度、电池（AXP2101）和 PWR 键、防烧屏调暗、**ES8311 音频**、
**7 种 UI 语言（默认英语）**、首次启动选择伙伴，以及一键**网页安装器**。

待完成：野外遭遇、3D 外壳、长时间稳定性测试。详见[路线图](#路线图)。

## 游戏手册（实际数值）

以下数值直接来自代码，描述游戏实际运行方式。

### 时间与等级

- **现实 1 分钟 = 游戏 1 分钟。** 每个现实小时升 **1 级**。升级只取决于时间；照顾
  良好不会加速，但疏忽会*推迟进化*。
- **100 级封顶**，需 4 天 3 小时。告别只在第 3 天（73 级）开始*提供*，并非期限；
  拒绝告别后仍可成长到 100 级，一天后会再次询问。
- 关机后 RTC 仍计时，最多补算 **2 周**成长。

### 四项状态（0–100）

需求为 **FOOD**（饱腹）、**JOY**（快乐）、**ENE**（能量）、**HYG**（卫生），初始
80 / 80 / 80 / 100。清醒时每分钟：

| 状态 | 每分钟下降 | 备注 |
|---|---|---|
| FOOD | −2 | |
| ENE | −1 | 体重 > 50 时额外 −1 |
| HYG | −1 | 屏幕上每份便便额外 **−4**，最多 3 份 |
| JOY | −1 | FOOD < 30 额外 **−2**；HYG < 30 再额外 **−2** |

- FOOD > 40 时每分钟约 **15%** 概率排便，卫生会迅速下降。
- 任意状态降至 **≤10** 计一次**照顾失误**（60 分钟冷却，避免重复计数）。每次失误
  让进化推迟 1 级，并使羁绊降低 1。

### 操作

- 🍎 **树果**（3 种口味）：FOOD +25。每种宝可梦有隐藏偏好口味，命中时 FOOD +35、
  JOY +10、增加爱心和羁绊，并揭示偏好。
- 🍬 **糖果：** FOOD +10、JOY +12，但体重 **+12**。
- ⚽ **球类/防御训练：** JOY +5，每次接球再 +2（最多 +35），每 2 次接球增加 1 点
  **DEFENCE**（单次最多 +18），消耗 ENE 并减重；提前离开仍保留实际成绩并进入答题。
- 🎯 **反应测试：** 在目标缩小消失前点击，训练 **SPEED**，难度逐渐提高。
- 🥊 **训练沙袋：** 约 4 击增加 1 点 **STRENGTH**，每次最多 +18，并消耗体力。
- 🫧 **洗澡：** 清除便便，HYG 恢复至 100。

选择喂食、洗澡或抚摸时会先弹出答题界面，正确结算后才播放对应动画；球类训练、反应测试
和沙袋训练则在训练结束后答题。选择题和四则运算题可以分别启用，只会出现已开启的题型；
两者都关闭时交互按 100% 直接执行，不弹出答题框。设备按当前界面语言从已安装 `.tquiz`
中通过索引随机读取选择题；没有匹配题目且四则运算题已开启时，按全局启用的运算符、项数、
位数、小数、分数和括号规则动态生成精确四则运算题。四则运算使用屏幕小
数字键盘直接输入答案，等值小数与分数均可判对；长题干可上下滚动。

答题时间固定划分为六个等长阶段。正确时按所在阶段取得正向效果的 **100%、83%、67%、
50%、33% 或 17%**；例如限时 30 秒、15 秒答对，最终效果为 50%。答错或超时为 0%：
喂食、洗澡和抚摸不产生效果或成功动画；训练仍保留实际成绩、能量/饱腹消耗与减重，但不
增加数值、快乐或羁绊；战斗招式完全失败。题型开关、限时、选择题比例和四则运算生成规则
在照顾、训练和战斗中全局共用一套配置。局域网对战允许双方使用不同开关：关闭答题的一方
立即提交 100%，另一方继续答题并保活，主机收到双方动作后统一结算。
- 👆 **抚摸：** 答题正确后按阶段获得最多 JOY +5，并增加羁绊。
- 🌙 **睡眠：** ENE 每分钟 **+6**；需求下降约慢 **4 倍**，且有 FOOD 30 / JOY 35 /
  HYG 45 下限。睡眠中不排便、不记失误、不会出走。**屏幕关闭且处于 00:00–06:00**
  会入睡，直到再次点亮屏幕才醒；系统每分钟复查。手动灯光按钮优先于自动规则。

### 蛋与孵化概率

- **第一个伙伴：** 从妙蛙种子、小火龙、杰尼龟中选择。
- 点击蛋 **3 次**即可孵化，也可等待自动孵化。
- 之后的蛋会在约 79 种可孵化基础形态中抽取稀有度：

| 稀有度 | 基础概率 | 正常告别后 | 物种数 |
|---|---|---|---|
| ✨ 传说 | ~3%\* | ~10% | 5 |
| 🔵 稀有 | ~27% | ~45% | 27 |
| ⚪ 普通 | 其余 | 其余 | 47 |

\* 图鉴登记 **≥25** 种后才会出现传说宝可梦。

- 每日连续照顾和高羁绊会提高稀有/传说概率。
- 正常**告别**会祝福下一枚蛋；**出走**会诅咒下一枚蛋，强制为普通。
- 同一稀有度内优先选择尚未完成进化线的物种，确保已安装地区可全部收集。
- **异色：** 基础 1/48，告别后为 1/24；连续照顾与羁绊最高可改善至 1/8，图鉴
  单独记录。
- 每次孵化都会生成独立 IV。

### 进化

**跨世代和分支进化由地区包提供。** 即便目标位于其他地区，大嘴蝠→叉字蝠、
大岩蛇→大钢蛇等关系仍会连接。已安装的 493 种目录覆盖臭臭花、蚊香君、呆呆兽、
伊布、无畏小子、刺尾虫、奇鲁莉安、土居忍士、雪童子、珍珠贝和结草儿等全部分支；
所有进化目标只能进化获得，不会直接从蛋中孵化。

`gen_dex_data.py --link` 会填充后来加入表格、且当前值仍为 0 的进化目标，不会重新
调整已有进化。神奥的电击魔兽、鸭嘴炎兽和超甲狂犀也采用同一规则。

- 达到进化等级（多数基础形态 16，石头类约 30，交换类约 40），且当时所有状态
  **≥40** 才会触发。
- **永不自动进化**：按钮出现后由你点击观看新旧形态闪烁动画。每次照顾失误推迟 1 级。
- 可选择“保持形态”，下一级再次询问。
- 分支物种优先选择已安装但尚未收集的进化形态。

### 队伍

- **告别**或**放生**后伙伴会加入最多 6 位的队伍，保留物种、昵称、异色、IV、训练和
  当前等级；加入后状态冻结，不再成长或训练。
- **出走不会加入队伍**，否则会消除疏忽的代价。
- 队伍已满时会直接进入选择页面，决定替换谁或让新成员离开，不会静默覆盖。
- 道馆与联机对战的可选队伍由当前伙伴和已入队伙伴组成。

### 三种结局（由你选择并见证，不会自动发生）

- 💛 **告别：** 最终形态且生活满 **3 天**后出现按钮；执行会祝福下一枚蛋。可选择
  “继续相伴”，一天后再次询问。
- 💔 **出走：** 四项状态全部保持 0 满一小时；任意一次照顾即可取消。会诅咒下一枚蛋，
  强制普通稀有度。
- 👋 **放生：** 长按伙伴，由你主动结束关系，效果中性。

任何结局后都会出现一枚**新蛋**。

### 羁绊、连续照顾、勋章与图鉴

- **连续照顾**属于玩家、跨伙伴保留：每天首次照顾推进；3/7/30/100 天有里程碑，漏一天
  即中断。
- **羁绊**属于当前伙伴，孵化时重置；亲密互动增加（每天最多 +20），疏忽降低。二者
  都改善蛋/异色概率及**下一位伙伴的 IV**。
- 每位伙伴有 **8 枚勋章**：10/25/50 级、发现偏好树果、连续 7 天、羁绊最大、最终形态、
  健美（体重 0 且无失误），另有全局计数。
- **图鉴：** 培育即登记；共 **493 种及异色**。按地区浏览，纵向切换关都/城都/丰缘/
  神奥，横向翻页，距首页最多十页。
- 蛋下的地区标签可选关都/城都/丰缘/神奥/全部；首次蛋提供该地区初始伙伴。未安装
  地区包的地区显示锁定且不进入蛋池；支持部分数据包安装。

### 战斗数值与 IV

每位伙伴孵化时生成四项不变的 **IV**（个体值，0–31）：ATK / DEF / SPD / VIT：

```
数值 = 基础值 + 等级 + (IV × 等级)/100 + 训练值
```

`(IV × 等级)/100` 是第三世代起的真实公式，满 IV 在 100 级贡献 +31；本项目中 IV
还会限制训练上限。

| | 效果 |
|---|---|
| 天生加成 | 正常生命周期终点 73 级时最多 **+22** |
| 训练上限 | `70 + (30 × IV)/31`：IV 8 为 **77**，IV 31 为 **100** |
| 总差距 | 约 **40 点**，优秀与较差个体相差约 **15%** |

- 实际 IV 为 **8–31，不会是 0**；伙伴只生活三天，避免低到惩罚玩家的废值。
- 上一位伙伴的连续照顾与羁绊可让下一次抽取上浮最多 +7。
- 传说宝可梦孵化时四项中保证三项满 IV。
- 异色宝可梦所有 IV 最低为 20，致敬第二世代异色与 DV 的关系。
- IV 显示在状态卡战斗页，31 会高亮。

训练方式：**STRENGTH** 来自沙袋，**SPEED** 来自反应测试，**DEFENSE** 来自球类游戏
（也会因一小时良好状态被动增加），**VIT** 无法训练。三项训练现均位于训练菜单。

每次孵化还会抽取 25 种**性格**之一。20 种非中性性格按原作对最终战斗数值分别进行
+10%/-10% 修正，HP 不受影响。原作中无修正的五种性格改为影响训练值贡献与每小时衰减：

| 性格 | 训练值贡献 | 受影响训练的每小时损失 |
|---|---|---|
| 勤奋 | 攻击 +10% | 攻击：上限的 9% |
| 坦率 | 防御 +10% | 防御：上限的 9% |
| 认真 | 速度 +10% | 速度：上限的 9% |
| 害羞 | 防御 +10%，攻击 -10% | 防御 9%，攻击 11%（各自上限） |
| 浮躁 | 攻击 +10%，防御 -10% | 攻击 9%，防御 11%（各自上限） |

未被性格影响的训练每满一小时损失其 IV 上限的 10%。每次都以上限而非当前值计算并
向上取整，在线与离线补算规则一致。攻击训练同时作为特攻训练贡献，防御训练同时作为
特防训练贡献，不另设特攻或特防训练值。

所有 **TM 在 40 级统一解锁**，此前均不可用。数据中的 TM 没有等级要求，但直接照搬
会让幼年伙伴利用空招式槽装入最强 TM，例如 1 级杰尼龟用冲浪和暴风雪击败小刚。
前五位馆主等级为 14–43，因此前半程使用物种自然学会的招式，后半程才开放 TM；伙伴
73 级可告别，100 级封顶。

招式表已加入抓、啄、毒针、泡沫、吸取、电光、乱击等低威力前期攻击；在此之前约
15% 的物种到 15 级仍无攻击招式，只能暗中依赖 TM 补位。

**道馆胜利也会训练数值**，让重复挑战有实际价值：

### 提前退休伙伴

正常告别只在最终形态且满三天后提供；菜单中的 **RETIRE** 可随时结束当前伙伴，采用
相同仪式并像告别一样将其存入队伍。

| | 代价 |
|---|---|
| 已满足告别条件 | 免费 |
| 尚未满足告别条件 | **下一位**伙伴晚一天进化 |

惩罚为 `EVO_PENALTY_LEVELS`（`MINUTES_PER_LEVEL 60` 时为 24），加入每个进化阈值，
与 `careMistakes` 的作用相同。债务落在下一只孵化的伙伴并在孵化时消耗，**不会叠加**：
连续三次提前退休仍只延迟一天。携带债务时卡片显示“晚一天进化”，确认框也会预先
说明代价。

### 选择蛋的地区

物种在蛋**出现**时决定，而非破壳时；切换地区会改变手中这枚蛋。两条规则避免刷蛋：

| | 规则 |
|---|---|
| 稀有度 | 已授予的稀有度**保留**，只改变同稀有度内的物种，无法刷传说 |
| 记忆 | 当前蛋会**记住**每个地区的结果，切回时仍是同一物种 |

新游戏最开始、选择伙伴之前先选择地区，因此首个伙伴与后续蛋来自同一地区；之后才
通过地区标签切换。地区归属由**基础形态**决定，进化会跨地区继续，例如关都流程仍可
得到叉字蝠和幸福蛋。

| | 胜利训练收益 |
|---|---|
| 简单 | **3–5** 点，挑战路线每深入 3 位馆主再 +1 |
| 困难 | **6–10** 点，采用相同路线加成 |
| 数值 | 在**尚未达到上限**的数值中随机 |
| 获得者 | 仅实际参战的当前伙伴 |

系统不会抽中已满数值，因此收益不会无声消失；IV 上限仍生效。已存入队伍的成员保持
冻结。对战本身会消耗当前伙伴能量，这而非冷却时间限制重复挑战；全部练满时会明确提示。

## 硬件

- 开发板：[ESP32-S3-Touch-AMOLED-1.75](https://www.waveshare.com/wiki/ESP32-S3-Touch-AMOLED-1.75)。请选择 **Standard**（无外壳）或兼容的 **-G**（GPS）版本；不要选带不兼容保护壳的 **-B**。另有“1.75**C**”是不同开发板。
- 466×466 圆形 AMOLED，**CO5300** 驱动（QSPI，80 MHz）。
- **CST9217** 电容触控（I2C 地址 0x5A）。
- **AXP2101** 电源管理/电池/PWR 键、**PCF85063** RTC、microSD 卡槽、**ES8311**
  音频编解码器（经功放连接 MX1.25 外置扬声器）。
- 引脚来自 [Waveshare 官方仓库](https://github.com/waveshareteam/ESP32-S3-Touch-AMOLED-1.75)，见 `pin_config.h`。

## 依赖库（Arduino IDE / arduino-cli）

| 库 | 作者 | 用途 |
|---|---|---|
| GFX Library for Arduino (`Arduino_GFX`) 1.6.4+ | moononournation | QSPI 驱动 CO5300，PSRAM 帧缓冲 |
| FreeType 2.14.3（裁剪后内置） | FreeType Project / Espressif component | 渲染 UI 包中的带 hint OpenType 字体 |
| SensorLib | Lewis He | CST9217 触控与 PCF85063 RTC |
| XPowersLib | Lewis He | AXP2101 PMU（电池、亮度、PWR 键） |
| ESP_I2S（ESP32 core 内置） | Espressif | 通过 I2S 驱动 ES8311 |

## IDE 设置与构建

- Board：**ESP32S3 Dev Module**；Flash：**16MB**；PSRAM：**OPI PSRAM**（466×466×16 位
  帧缓冲约 434 KB，必须放在 PSRAM）；选择带 FAT 的分区方案，例如
  `16M Flash (3MB APP/9MB FATFS)`；**USB CDC On Boot** 设为 **Enabled**。

```bash
FQBN="esp32:esp32:esp32s3:CDCOnBoot=cdc,FlashSize=16M,PSRAM=opi,PartitionScheme=app3M_fat9M_16MB"
TAMAPOKE_VERSION="$(python3 tools/firmware_version.py)"
FW_DEFINE="$(TAMAPOKE_VERSION="$TAMAPOKE_VERSION" python3 tools/firmware_version.py --cpp-define)"
arduino-cli compile --fqbn "$FQBN" \
  --build-property "compiler.cpp.extra_flags=$FW_DEFINE" .
arduino-cli upload -p /dev/cu.usbmodemXXXX --fqbn "$FQBN" .

# 或使用会自动寻找端口并可打开串口监视器的脚本：
bash tools/flash.sh --monitor
```

受支持的本地构建脚本会将最新提交的短 ID 和 UTC 构建时间写入固件版本。GitHub Pages
发布构建则原样使用已发布 GitHub Release 的标签。

### 在电脑上运行

`tools/emu/` 会把未经修改的**真实固件**（`TamaPoke.ino`、`pet.cpp`、`i18n.cpp`、
`party.cpp`）编译为可交互桌面应用，只替代硬件接口。点击模拟触控，拖动模拟滑动，
在终端输入串口命令；`--fast 60` 可将三天生命周期压缩到约一小时。

```bash
brew install sdl2 freetype # Debian: apt install libsdl2-dev libfreetype-dev
bash tools/emu/build.sh
tools/emu/tamapoke-emu --scale 2 --fast 60
```

还可使用无界面的 `--shot` 截图模式，详见 [`tools/emu/README.md`](tools/emu/README.md)。
模拟器无法验证时序、DMA 撕裂、PSRAM 或音频，这些仍需真机测试。

### 最简单的安装方式：网页安装器

`web/index.html` 通过 ESP Web Tools 刷写固件，再通过 Web Serial 向 SD 部署 UI、招式、
地区和题库包，无需 Arduino。页面链接的 `web/question-bank.html` 可以从文件或已连接设备
导入、编辑、搜索、分页、导出、构建并直接部署索引题库；内部包 ID、题目 ID 和版本均由
页面自动维护，也能读取和写入设备的全局题型开关与答题规则。请通过 HTTPS 或 `localhost`（安全上下文）提供页面，并使用
**Chrome/Edge** 打开。详见 [`web/README.md`](web/README.md)。

迁移到运行时分包会有意重置旧固件存档；未知存档结构会被清除，不会尝试按新目录布局解释。

### 自行生成并加载数据包

所有精灵来自 **[PMD SpriteCollab](https://github.com/PMDCollab/SpriteCollab)**
（CC BY-NC）。`gen_data_packs.py` 直接读取逐物种生成的 TPK2/TPTH 文件，将其与 UI、
物种、招式、说明、训练家、战斗和徽章数据合并，不生成地区中间包。

```bash
python3 tools/pack_pmd.py       # 获取并打包当前目录及异色输入
python3 tools/make_thumbs.py    # 从 PMD 精灵生成图鉴缩略图 -> thumbs.bin
python3 tools/gen_data_packs.py # web/packs/*.tui、*.tmove、*.tregion、*.tquiz + index.json
python3 tools/check_data_packs.py
```

仓库内的中文字体子集已覆盖目录中的所有字符串、物种、招式、属性和地区说明。新增本地化
字符后，先用 FontTools 从 Noto Sans CJK SC Medium 重建字体，再重新生成数据包：

```bash
python3 tools/subset_ui_font.py /path/to/NotoSansCJK-Medium.ttc \
  tools/assets/fonts/NotoSansCJKsc-Medium-subset.otf --font-number 2
```

随后通过网页安装器部署所选包。固件只接受 `/packs` 下通过验证的包文件；中断上传使用
临时文件，不会覆盖工作包。

题库源 JSON 放在 `tools/question_banks/`；生成器会把每份源文件编译为带索引的 `.tquiz`
并写入网页目录。浏览器题库构建器无需 Python，也会生成完全相同的二进制格式。

## 游戏方法

首次运行时从妙蛙种子、小火龙、杰尼龟中选择初始伙伴，之后从一枚蛋开始。点击三次或
等待它自动孵化，然后照顾伙伴。

四项会下降的状态为 **FOOD / JOY / ENE / HYG**；任一归零会记为一次失误。

**底部弧形按钮：**

- 🍎 **喂食：** 三种树果与糖果；偏好树果有额外收益，糖果增加快乐但会增重。
- ⚽ **玩耍：** 精灵球小游戏，只增加快乐。
- 🌙 **灯光：** 睡眠/唤醒；睡眠恢复能量、调暗屏幕并减慢需求下降。
- 🫧 **洗澡：** 播放泡沫动画并清理便便。

**触控手势：**

- 点击顶部名字打开菜单（图鉴/队伍/设置）；点击 CLOSE、面板外或任意滑动关闭。
- 点击伙伴进行抚摸，增加快乐与羁绊。
- 横向滑动打开图鉴；向上滑动打开状态卡（资料/战斗/勋章/进度），页间继续滑动；
  资料页点名字可改名，战斗页“训练力量”打开沙袋。
- 向下滑动设置时钟、语言与声音开关。
- 长按伙伴 3 秒打开释放对话框。

**实体 PWR 键：** 短按开关屏幕；长按 4 秒完全关机。RTC 保持运行，关机期间时间仍推进。

## 决定由你作出，也由你见证

三种生命周期结局和进化都**不会自行发生**。条件满足后出现按钮，由你点击并在双选项
对话框中决定：

- **进化**（红色）：选择进化可观看光环、光线、闪光以及新旧形态闪烁动画；也可保持
  形态，下一级再次询问。
- **告别**（金色，最终形态且满三天）：选择告别会播放爱心上升并出现新蛋；也可继续
  相伴，一天后再问。这是在满级伙伴与完成图鉴之间的取舍。
- **出走**（暗色，四项状态为 0 满一小时）：播放雨中“感到被遗弃”的结局；在此之前
  进行任意照顾即可取消。

  **出走不会询问许可，这正是设计目的。** 但它不能成为睡觉的代价，因此屏幕关闭且
  时间在 00:00–06:00 时伙伴会睡眠；FOOD/JOY/HYG 下限为 30/35/45，并完全跳过疏忽
  检查。早上面对的是饥饿、脏乱的伙伴，而非空设备。

  屏幕状态与时间窗口缺一不可：只看屏幕会让白天收进口袋时暂停游戏，只看时间会在你
  仍游玩时强制睡眠。请在 **SETTINGS** 或通过 `RTCSET` 设置时钟；未设置时钟的设备
  不会自动睡眠，但手动灯光仍可用。

  该规则源于真实丢失事件：设备通宵运行后迷你龙出走。实时 tick 曾是唯一没有下限的
  消耗路径（离线下限 15，睡眠为 30/35/45），导致开机反而受罚。`night_test` 模拟十
  小时，防止问题回归。

## 运行时数据包

- **UI（`.tui`）：** 每包一种语言，包含字符串、布局参数，以及紧凑位图字体或由包定义
  像素尺寸的带 hint OpenType 子集；启动时动态发现语言列表。
- **招式（`.tmove`）：** 稳定招式 ID、机制、学习表/TM、属性克制、名称与本地化说明。
- **地区（`.tregion`）：** 物种记录、本地化名称与说明、PMD 精灵、缩略图、地区元数据、
  训练家、地区战斗配置与徽章。
- **题库（`.tquiz`）：** 语言范围、定长随机访问索引和变长题目记录。固件每次只读取被选中
  的一行索引与一道题目，不会把整个题库载入内存。

精灵按需从地区包读入 PSRAM；OpenType 字体及有界字形缓存也在 PSRAM 中。固件不嵌入
字体或宠物精灵，也没有散文件回退。缺少必需包时进入内置 USB 恢复界面，而非用残缺
目录启动。

## 图鉴与物种数据

`tools/dex_data.py` 是名称、slug、属性（强调色与背景生态）、第一世代风格进化等级、
稀有度和初始伙伴的数据源。`tools/dex_stats.py` 提供基础数值，`tools/dex_types.py`
提供属性与克制表，后两者来自 PokéAPI。这里采用**当前数值**而非第一世代数值，例如
大比鸟速度为 101 而非红/蓝版的 91。生成器把记录写入地区包；`dex.h` 只保留稳定 ABI
与上限。伙伴身份以存入 NVS 的图鉴编号表示。

- 进化采用第一世代风格：16/36 等级、石头约 30、交换约 40；伊布优先分支到尚未收集
  的形态。每次失误推迟 1 级；任意状态 <40 或睡眠中不能进化。

## 属性

每种宝可梦携带真实的一到两种属性。游戏在招式包中提供由 `tools/dex_types.py` 生成的
完整 **18×18 克制表**。

克制采用**当前第六世代及以后规则，而非第一世代**：

- 第一世代存在幽灵招式对超能力无效等实际错误，且超能力几乎无制衡。
- 基础数值已采用 PokéAPI 当前值，配古代克制表会不一致。
- 妖精属性有实际价值：快龙攻击最高；第六世代前龙只被钢抵抗，而龙→妖精为 **0×**，
  皮可西可完全克制可孵化的最强攻击者。

最初 151 种中有 7 种属性不同：小磁怪/三合一磁怪在第二世代获得钢；皮皮、皮可西、
胖丁、胖可丁、魔墙人偶在第六世代获得妖精，后五者中的前两只失去一般属性。

属性显示在状态卡战斗页。*（战斗：列于路线图。）*

## 战斗数值与训练

ATK/DEF/SPD/VIT = **基础值** + 等级 + **IV**（孵化时 0–31，按真实公式
`IV × 等级/100`）+ **训练值**，再应用性格修正（见[战斗数值与 IV](#战斗数值与-iv)）：

- SPEED ← 反应测试（约 2 次反应 +1，每次最多 +18）。
- DEFENSE ← 良好状态累计（休息或照顾良好 1 小时 +1）。
- STRENGTH ← 训练沙袋（约 4 击 +1，每次最多 +18）。
- VIT（来自基础 HP）不可训练。

### 招式

每位伙伴最多掌握 **4 个招式**，总池 77 个，分两类：

- **升级招式**有等级门槛，例如喷火龙 34 级喷射火焰、36 级翅膀攻击、54 级龙之怒。
  幼体只带该物种 1 级可学招式，小火龙仅有叫声，其余槽为空。达到门槛且有空槽时
  自动填入；四槽已满则提示遗忘一个或跳过。离线成长跨过多个门槛时依次排队询问。
  进化保留已有招式，改用新形态门槛；低于当前等级的漏学招式不会补学，与原作一致。
- **TM** 无独立等级门槛，可在状态卡 **MOVES** 页按需选择（滑到该页后点击招式槽）。

等级来自仍严格使用门槛的火红/叶绿。一个招式同时是 TM 时仍保留其升级门槛，否则多数
曾作为 TM 出售的升级招式都会免费获得。

伙伴存入队伍后，招式与等级、训练一起**冻结**；生前选择的配置会永久用于战斗。

**特攻与特防**使用物种自己的 `bSpA`/`bSpD`（例如胡地攻击 50、特攻 135），但复用
物攻/物防的 IV 与训练，不额外抽取：特攻使用 ATK IV/训练，特防使用 DEF IV/训练。
物理/特殊差异属于物种，而非再给个体增加一组 IV。

IV 也决定每项数值可训练的上限（77–100），高 IV 不只起点更高，上限也更高。数值见
[战斗数值与 IV](#战斗数值与-iv)。战斗页显示这些数据；隐藏体重随糖果增加、随训练减少。

## 留存机制：连续照顾、羁绊、勋章与昵称

- **连续照顾**属于玩家并跨伙伴保留；每天首次照顾推进，3/7/30/100 天庆祝，漏一天
  中断；主屏显示火焰徽标。
- **羁绊**属于伙伴，随照顾与抚摸缓慢增加，失误时下降。
- **勋章**记录个体的等级、树果、连续照顾、羁绊、最终形态、健美等成就，另有全局计数，
  显示于状态卡勋章页。
- **昵称**通过触控键盘输入，并用于标题和卡片。

高连续照顾与羁绊会改善蛋的稀有度与异色概率，良好照顾始终有回报。

## 生命周期、蛋的稀有度与语言

生命周期为 **3 天**。三种结局都会留下新蛋：告别（最终形态 + 3 天）、放生（长按）、
出走（四条状态为 0 满 1 小时）。培育过的物种会登记到培育图鉴，普通与异色分开记录。

蛋在约 79 种基础形态中抽取（47 普通 / 27 稀有 / 5 传说），偏向尚未完成的进化线；
正常告别提高概率，出走降低概率。登记 25 种后才可出现传说。异色基础为 1/48，可由
连续照顾、羁绊和告别改善。

**语言：** 官方包包含英语（默认）、西班牙语、法语、德语、意大利语、葡萄牙语和
简体中文。固件不硬编码该列表；设置中只显示已安装的 `.tui` 包。

## 背景：生态与真实时间

待机画面的天空来自 RTC 实时时间（黎明/白昼/黄昏/带月亮星空的夜晚），地面来自属性
生态（草地、海滩、森林、火山、山地、雪地）；睡眠强制使用夜景。

## 项目结构

- `TamaPoke.ino`：初始化、游戏循环、所有屏幕渲染、手势、串口控制台、音频。
- `pet.h` / `pet.cpp`：伙伴状态与逻辑（状态、进化、生命周期、连续照顾/羁绊/勋章、NVS）。
- `party.h` / `party.cpp`：由告别和放生保留的 6 位退休伙伴。
- `content.h` / `content.cpp`：包 ABI、CRC 校验、目录、说明与延迟加载资源。
- `quiz.h` / `quiz.cpp`：全局答题规则、精确四则运算生成/输入与限时效果结算。
- `sdmon.h` / `sdmon.cpp`：TPK2 精灵、缩略图与 USB 原子接收分包。
- `rtcbat.h` / `rtcbat.cpp`：PCF85063 RTC、AXP2101 PMU、电池、亮度与 PWR 键。
- `audio.h` / `audio.cpp`：ES8311、I2S、Game Boy 风格非阻塞音调合成任务。
- `i18n.h` / `i18n.cpp`：动态选择已安装语言与字符串 ID。
- `dex.h` / `moves.h`：稳定固件 ABI；目录记录位于 SD。
- `ui_art.h`：生成的核心 UI 图标/颜色；宠物精灵仅存在于地区包。
- `pin_config.h`：开发板官方引脚。
- `tools/`：数据与生成流水线，包括 `dex_data.py`、`dex_stats.py`、`dex_types.py`、
  `sprites.py`、`pack_pmd.py`、`make_thumbs.py`、`gen_data_packs.py`、`quiz_pack.py`、校验器与 `touch_log.py`。
- `tools/emu/`：使用真实固件与 SDL 硬件桩的桌面模拟器。
- `tools/sdcard/mons/`：生成的动画/异色 PMD 精灵与缩略图输入。
- `web/packs/`：可部署 UI、招式、地区、题库包及动态目录。
- `web/`：浏览器安装器与题库构建器，提供 ESP Web Tools、Web Serial 部署和规则配置。

## 串口控制台（115200，调试）

`STATS`（完整状态）· `SPEC <dex>`（更换物种）· `LVL <n>` ·
`IV <atk> <def> <spd> <vit>`（指定个体值）· `HATCH` ·
`EGG <dex> [shiny]`（孵化指定物种；用于测试孵化时传说/异色 IV 保证）·
`SHINY` · `NICK <x>` · `BYE` / `RUN`（告别/出走）· `ABANDON`（强制进入出走就绪）·
`WIPE`（恢复出厂并开始新游戏）· `BEEP`（音频测试）· `REG`（图鉴）·
`EGGS`（模拟 20 枚蛋）· `GAL`（画廊）· `CAREDAY` ·
`PARTY` / `PARTY <dex>` / `PARTY CLEAR`（查看/填充队伍）·
`TIME <epoch>` / `RTCSET <epoch>` · `HEALTH`（稳定性测试的运行时间与堆）·
`LS` / `GET` / `PUT`（列出、读取或部署通过验证的 `/packs` 文件）· `QUIZCFG`（读取全局答题规则）·
`QUIZSET <time> <ops> <terms> <operandDigits> <answerDigits> <decimals> <fractionDigits> <flags> <parenthesisDepth> <choiceWeight> <questionTypes>`，其中 `questionTypes` 为位掩码：选择题 `1`、四则运算题 `2`。

快速测试时可降低 `pet.h` 中的 `PET_TICK_MS`、`MINUTES_PER_LEVEL` 和
`FAREWELL_AGE_MIN`。

## 路线图

- **野外遭遇/战斗：** 已设计；用 ATK/DEF/SPD 与 PMD Attack/Hurt 动画结算，训练家
  段位作为终局内容；仍需选择自动、时机或回合制风格。
- **24–48 小时稳定性测试：** 插桩已就绪，可用 `HEALTH` 命令/心跳。

*（已完成：3D 打印外壳已[发布到 MakerWorld](https://makerworld.com/es/models/2937822-tamapoke-a-pokemon-pokeball-tamagotchi)；仓库已公开，并提供浏览器安装器与一键精灵包。）*

## 社区分支

- **ShadowEnemy** 的 **[TamaPoke — Expanded](https://github.com/ShadowEnemyx/TamaPoke/tree/tamapoke-expanded-update)**：由不同作者/分支维护的大型社区版本，包含完整属性克制战斗、151 种及异色、图鉴/收集盒、每日目标、6 种 UI 语言、ES8311 音频、初始伙伴选择与一键网页安装器，值得一看。🎮

## 致谢

全部精灵来自社区项目 [PMD SpriteCollab](https://github.com/PMDCollab/SpriteCollab)
（CC BY-NC）；基础数值来自 [PokéAPI](https://pokeapi.co)。Pokémon 是 Nintendo /
Game Freak / The Pokémon Company 的商标。本项目仅供非商业个人使用。完整列表见
[`CREDITS.md`](CREDITS.md)。

## 许可证

- **源代码**（固件与工具）：**[MIT](LICENSE)**。
- **精灵与名称：** © Nintendo / Game Freak / The Pokémon Company；像素画来自
  [PMD SpriteCollab](https://github.com/PMDCollab/SpriteCollab)（CC BY-NC 4.0），
  **仅限非商业使用**。
- **3D 打印外壳：** 基于 **yoyothechicken** 的 *“Pokeball”*
  （[MakerWorld #839922](https://makerworld.com/es/models/839922-pokeball)）改作，采用
  **CC BY-NC-SA**，并以相同条款分享。

这是非官方同人项目，与 Nintendo 无隶属关系，也未获其认可。
