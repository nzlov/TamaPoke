# AGENTS.md

本文件适用于整个 TamaPoke 仓库，用于约束在本项目中工作的编码代理。子目录若有更具体的 `AGENTS.md`，以更深层文件为补充；与系统、开发者或用户的即时指令冲突时，遵循更高优先级指令。

## 对话与上下文焦点

- 每次直接回复用户时，开头必须先称呼“主人”。忘记称呼即视为失焦，需要手动重置当前上下文焦点后再继续。
- 不要发送可选的 commentary；只有工具调用前的必要说明、重要进度、风险、审批或阻塞信息才发送 commentary。
- 先给结论，再说明必要细节。不要用冗长格式掩盖不确定性。
- 不确定需求、验收标准、执行取舍或下一步时，停止实现，明确列出不同理解及其影响，并向用户提问。

## 编码前先思考

不要想当然，不要掩盖疑惑，明确说出取舍。

开始实现前必须：

- 明确写出假设；不确定就提问。
- 存在多种合理理解时，不要私自选择，先说明各自影响。
- 有明显更简单的做法时直接指出，必要时提出异议。
- 定义可验证的成功标准，再开始修改。
- 先检查工作树状态和相关文件，保留用户已有改动，不覆盖、不清理无关内容。

## 架构取舍

- 制定计划或实现前，主动判断是否存在比局部补丁更清晰的领域边界或数据流。
- 如果方案会引入胶水代码、重复 mapping、透传 wrapper、adapter、shim、薄 orchestration、职责错位、过宽 API 或反复转换数据形状，先判断根因是否是边界设计问题。
- 对重构、复杂 bug、跨模块修改和长期维护问题，计划中先给出推荐架构方案，再评估最小补丁。
- 推荐架构更简单、胶水更少且符合领域边界时，默认采用推荐架构；但若会明显扩大范围、改变公共接口、影响多个模块或需要迁移调用方，必须先询问用户。
- 架构优化只能服务当前目标，不得扩展到无关模块或泛化重构。
- 无法避免的胶水代码必须在附近添加简短的 `GLUE:` 注释，说明连接的边界、存在原因和可删除条件；不要给显而易见的赋值或一次性简单转换添加噪音注释。
- 一次任务中出现多个 adapter、shim、wrapper、重复 mapping 或跨模块透传时，暂停并重新评估是否应重构；若重构超出已确认范围，先询问用户。

## 简单优先

- 只写解决当前问题所需的最少代码，先消除复杂度来源，再决定是否抽象。
- 不添加请求之外的功能、配置项、扩展点或不现实场景的错误处理。
- 优先使用语言标准库、平台内置能力和项目现有依赖；不要为轻量封装新增第三方包或自定义工具层。
- 只使用一次、且在调用处能清楚表达的逻辑，不新增 helper、utility layer 或 utility package。
- 只有两个以上调用点会长期共同演进、业务含义一致，或抽取能显著降低错误面时，才提取共享实现。
- 共享逻辑优先归入已有领域模块或工具包；归属清晰且复用稳定时才新增包。
- 提取共享逻辑后删除因本次修改产生的重复实现，并用最窄测试验证行为未漂移。
- 如果 50 行可以解决，不写 200 行。持续问自己：“资深工程师会觉得这过度复杂吗？”

## 外科手术式修改

- 只改完成请求所必需的内容，每一行 diff 都应能追溯到用户目标。
- 不顺手优化相邻代码、注释、命名或格式，不重构没有坏掉的部分。
- 匹配现有代码风格，即使个人偏好不同。
- 发现无关死代码可以指出，但未经请求不要删除。
- 删除因本次修改而变成未使用的 import、变量、函数或分支；不要清理原本就存在的无关问题。
- 禁止占位实现、未实现分支、空函数、stub、`panic("TODO")`、`throw new Error("not implemented")`，以及以说明或 TODO 代替真实行为。
- 不得跳过、弱化或删除测试来制造通过结果。

## 目标、计划与验证

- 将任务改写成可验证目标，例如“修复 bug”应变成“先用测试复现，再使测试通过”。
- 多步骤任务必须使用运行环境提供的结构化计划工具维护 TODO；若需要向用户展示，也使用以下可验收的 checklist 语义：

  ```text
  - [ ] [可执行步骤] → verify: [测试、命令、diff、日志、截图或可见行为]
  - [x] [已完成步骤] → verify: [已实际通过的验证]
  ```

- 每项必须是可执行动作，并带明确验证方法；不要写“处理好”“优化一下”等无法验收的事项。
- 状态必须随真实进度更新。只有验证实际通过后才能标记完成；失败时保持未完成，记录结果并继续修复或询问。
- 优先运行与改动最相关的窄测试，再按风险扩大到完整测试。
- 测试失败时先判断是本次修改、环境缺失还是既有失败；不要掩盖或误报。
- 完成交付前检查最终 diff、未跟踪文件和测试输出，确保没有临时产物、意外生成文件或用户改动被覆盖。

## 项目概览

TamaPoke 是 Waveshare ESP32-S3-Touch-AMOLED-1.75 上运行的 Arduino/C++ 电子宠物固件。仓库还包含 Python 数据生成流水线、使用真实固件源码和硬件桩的 SDL 桌面模拟器，以及浏览器安装器。

主要边界：

- `TamaPoke.ino`：初始化、主循环、屏幕渲染、触摸手势、串口控制台和顶层 UI 状态。
- `pet.h` / `pet.cpp`：伙伴状态机、数值衰减、生命周期、进化和培养逻辑。
- `party.h` / `party.cpp`：六个培养槽、当前成员、旧存档迁移和 24 格 Box。
- `battle.h` / `battle.cpp`、`wild.h` / `wild.cpp`：回合制战斗和野外遭遇规则。
- `content.h` / `content.cpp`：运行时数据包 ABI、校验、索引和延迟加载。
- `save.cpp`、`inventory.cpp`、`items.cpp`：持久化、背包和道具。
- `i18n.h` / `i18n.cpp`：字符串 ID 和运行时本地化。
- `audio.cpp`、`gbsynth.cpp`：硬件音频与可测试的合成核心。
- `rtcbat.cpp`、`sdmon.cpp`、`linknow.cpp`：RTC/电源、SD/USB 和 ESP-NOW 硬件边界。
- `dex.h`、`moves.h` 及 `web/packs/`：生成数据和稳定的数据 ABI。
- `tools/`：生成器、校验器、刷写工具和 Web 构建流程。
- `tools/emu/`：编译真实固件源码的 SDL 模拟器及宿主硬件桩。
- `tools/emu/tests/`：针对真实固件类和 UI 路径的宿主测试。
- `web/`：ESP Web Tools 安装器、Web Serial 工具和题库页面。
- `src/tamafreetype/`、`freetype/`、`third_party/`：嵌入式字体与第三方代码；非任务必要不要修改。

先阅读与任务直接相关的代码和文档。`README.md` / `README.zh-CN.md` 是用户功能与实际数值说明，`CLAUDE.md` 和 `HANDOVER.md` 记录历史陷阱，但历史状态可能过期；以当前源码、测试和生成器为最终依据。

## 项目硬规则

### 固件与硬件

- 目标板配置来自 `sketch.yaml`：ESP32-S3、16 MB Flash、OPI PSRAM、`app3M_fat9M_16MB` 分区。466×466 RGB565 帧缓冲依赖 PSRAM，不得随意改成无 PSRAM 配置。
- `pin_config.h` 来自官方板级定义，不凭猜测修改引脚。
- 模拟器不能验证真实时序、DMA 撕裂、PSRAM 压力、电池、无线电、真实触控命中率或最终音频效果；涉及这些边界必须说明还需真机验证。
- `TamaPoke.ino` 作为大 sketch 是当前设计。除非任务明确要求，不以“模块化”为由大范围拆分。

### 稳定数据与存档 ABI

- 任何保存图鉴编号的字段使用足够容纳当前 `DEX_COUNT` 的类型；不要使用 `uint8_t`，不要写死 `151`，循环上限应来自 `DEX_COUNT` 或对应源数据。
- 招式索引直接持久化到 NVS。`moves.h` / 生成源中的新招式必须追加到 `APPEND-ONLY BELOW HERE` 之后，不得插入、重排或重新解释已有索引。
- 地区、数据包和界面列表必须从单一权威来源派生，不在 Python、C++ 和 JavaScript 中分别维护重复列表。
- 修改保存字段、包格式、偏移、枚举或索引前，先找到所有生产者、消费者和迁移路径，并补充兼容性测试。

### 本地化与文档

- 新增 UI 字符串时，在 `i18n.h` 中按顺序添加 `StrId`，并保证所有运行时语言包提供相同位置的完整字符串目录；不得只更新单一语言。
- 默认 UI 语言为英语。源代码注释和提交信息使用英语；不要为了翻译而改动既有西班牙语注释。
- 平衡数值、概率、阈值和用户可见行为发生变化时，同步更新 `README.md` 与 `README.zh-CN.md` 中对应说明。
- README 更新使用肯定句描述已实现和当前可用的行为，省略未实现、已移除或本次未涉及的事项。
- 不要在 `TamaPoke.ino` 硬编码发布版本；受支持的构建流程通过 `tools/firmware_version.py` 注入版本。
- 生成文件应通过其生成器修改，不手工维护容易被下一次生成覆盖的输出。修改前先确认文件头、脚本或文档中的来源说明。

### Web 安装器与玩家存档

- Web 更新必须保留 NVS。安装器清单继续使用 bootloader、partitions、boot_app0、app 四段及各自偏移，不能改成从偏移 0 写入的单一合并镜像。
- `web/manifest.json` 的 `new_install_prompt_erase` 必须保持 `true`；该名称容易误导，设为 `false` 会绕过询问并整片擦除。
- 任何 Web 固件或 manifest 修改都必须运行 `python3 tools/check_installer.py`；完整发布构建使用 `bash tools/build_web.sh`。
- `tools/build_web.sh` 会重建并改写固件、manifest 和数据包。只有任务确实需要这些产物时才运行，并在运行后检查所有生成 diff。

## 构建与测试

### 依赖

- 固件构建使用 `arduino-cli` 和 `sketch.yaml` 中固定的 ESP32 平台及库版本。
- 模拟器需要 SDL2、FreeType、支持 C++17 的 `g++`、Python 3 和 `pkg-config`。
- 缺少依赖时报告缺失项；安装依赖或访问网络前遵循当前环境的审批规则。

### 固件编译

优先使用 profile：

```bash
arduino-cli compile --profile esp32s3 .
```

若工作树目录名不满足 Arduino sketch 目录必须与 `.ino` 同名的限制，使用项目已有 `tools/emu/tests/run.sh` 或 `tools/build_web.sh` 中的临时镜像方式，不要重命名或搬动当前工作树。

### 桌面模拟器

```bash
bash tools/emu/build.sh
tools/emu/tamapoke-emu --scale 2 --fast 60
```

- 模拟器编译真实的 `TamaPoke.ino` 和核心 `.cpp`；只有硬件层由宿主桩替代。
- UI、招式、物种和精灵数据来自 `web/packs/`。数据包相关修改后先运行对应生成器和校验器。
- 模拟器生成的 `tools/emu/sketch.cpp`、`tools/emu/proto.h`、可执行文件、NVS 和截图属于构建/测试产物；除非任务明确要求，不提交它们。
- 模拟器能编译不代表 Arduino 固件能编译，因为模拟器预先生成所有函数声明；可用时必须让 `arduino-cli` 编译验证。

### 自动化测试

```bash
bash tools/emu/tests/run.sh
bash tools/emu/tests/run.sh battle
```

- 无参数运行全部宿主测试；参数按测试名过滤。先运行相关过滤测试，再根据风险运行全量测试。
- 测试脚本会先构建模拟器；本机存在 `arduino-cli` 且未设置 `SKIP_FIRMWARE=1` 时，也会编译真实固件。
- 测试应断言真实业务类或真实 UI 调用路径，不要在测试中重新实现被测公式。
- 新增保护规则时应进行负向检查：临时破坏实现并确认测试确实失败，再恢复并确认通过。无法失败的断言不是有效回归保护。
- 新增分页页面必须加入 `swipe_test`；修改渲染或页面切换时关注 `flush_test`；修改本地化目录时关注 `i18n_test`。
- `--shot` 直接读取帧缓冲，无法发现遗漏 `gfx->flush()` 的冻结问题，截图不能替代 `flush_test`。

常用数据和 Web 校验入口按改动范围选择：

```bash
python3 tools/check_data_packs.py
python3 tools/check_installer.py
python3 tools/check_pages_site.py
python3 tools/quiz_pack_test.py
node tools/question_bank_web_test.mjs
```

不要无依据运行会下载大量资源或重建全部生成物的脚本。需要运行时，先确认任务范围和当前工作树状态。

## 模拟器测试截图要求

- 每次使用模拟器进行功能、交互或视觉测试时，必须截取能够证明测试结果的截图；仅口头说明“模拟器验证通过”不算完成。
- 测试涉及多个关键页面、弹层、语言、状态或前后变化时，分别截取足以覆盖验收点的画面，不用无关截图堆数量。
- 优先使用可重复的无头截图模式：

  ```bash
  tools/emu/tamapoke-emu --shot <scene> --lang zh-CN --out <output>.ppm
  ```

- 截图应转换为用户可直接查看的常见格式（优先 PNG），检查画面内容正确后，在交付说明中提供可点击的截图或明确路径。
- 在 AnyCode 环境中，截图、转码文件及其他临时验证产物写入 `$ANYCODE_ARTIFACT_DIR`，不要写入项目工作树。只有用户明确要求更新项目截图文档时，才修改 `docs/screens/`。
- 截图证据必须与自动化断言配合：布局和可见状态使用截图，业务规则使用测试，真实硬件边界使用真机验证。

## 反复出现的项目陷阱

- 修改一条规则时，用 `rg` 找出所有调用方，优先让它们调用同一个权威函数；不要在多个 UI 或业务路径复制判断。
- 测试调用方，而不只测试底层规则。底层 helper 正确但界面绕开 helper，仍然是用户可见 bug。
- 断言规则和不变量，不要只固定某个恰好成立的数值。检查测试输入确实触发了目标路径。
- 合成触控坐标不能证明真实手指容易点中。不可逆操作附近的控件需要更大的命中区和安全 guard band，并用真机检查。
- 大时间跳变、离线恢复和 NVS 迁移必须从已知状态开始测试，避免前一测试的持久化数据污染结果。

## Git、文件与安全

- 使用 `rg` / `rg --files` 搜索文件和文本。
- 使用补丁工具进行手工编辑；不要通过临时脚本进行难以审查的源码改写。
- 不执行 `git reset --hard`、`git checkout --`、递归删除或其他破坏性操作，除非用户明确要求且目标已核实。
- 不删除、移动、重建或清理当前工作树。
- 不覆盖用户已有修改；遇到与任务重叠的脏文件时先读 diff，再决定能否安全合并。
- 不提交、不推送、不创建 PR，除非用户明确要求。
- 功能开发使用功能分支，不直接向 `main` 提交。
- 临时文件放在安全的临时目录或当前环境指定的 artifact 目录，任务结束前检查是否误写入仓库。

## 子代理使用

- 使用子代理前先根据任务性质选择模型；除非用户明确指定，否则不要临时改用其他模型。
- 只读查询任务使用 `gpt-5.4-mini`，包括资料检索、代码阅读、日志或配置查询和事实核对；此类子代理不得修改文件或执行有副作用的命令。
- 实现、修复、重构、测试编写、计划执行、代码审查、质量审查、安全审查、回归风险审查，以及任何可能修改文件或外部状态的任务，使用与主代理一致的模型。
- 派发提示词不要以“主人”开头；称呼规则只适用于代理直接回复用户。
- 派发时明确任务是否只读、允许访问范围、预期产出和禁止修改边界。
- 子代理完成实现、修复或测试后，主代理必须检查实际 diff 和测试结果，不能只相信总结。
- 检查占位实现、未实现分支、TODO/FIXME、stub、空函数、只改说明不改行为，以及测试被跳过或弱化等信号；发现后补齐并重新验证。
- 只有用户或更高优先级指令允许委派时才使用子代理。

## 完成标准

任务只有同时满足以下条件才算完成：

- 用户请求对应的可见行为或文档已经实现，没有扩展无关范围。
- 相关窄测试通过；按风险需要的全量、固件、数据包或 Web 校验也已完成。
- 使用模拟器测试时，已有经检查的截图证据，并在交付说明中提供。
- 最终 diff 经过人工核对，不含意外格式化、无关重构、临时文件或生成物污染。
- 没有占位实现、跳过测试、未说明的失败或被掩盖的硬件验证缺口。
- 最终回复以“主人”开头，先说明结果，再列出验证及任何仍需真机完成的事项。
